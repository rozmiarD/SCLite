[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "security-contract-layer"
version = "0.1.0"
description = "JSON-first security contract artifacts, validation, and public-safe proof fixtures"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "Ravenclaw Security Contract Layer maintainers" }]
keywords = ["security", "contracts", "validation", "evidence", "governance"]
classifiers = [
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3 :: Only",
]
dependencies = []

[project.optional-dependencies]
dev = [
  "pytest>=8,<9",
]

[project.scripts]
scl = "scl.cli:main"

[tool.setuptools]
packages = ["scl"]
include-package-data = true

[tool.setuptools.package-data]
scl = [
  "schemas/*.json",
  "examples/security-contract-proof/*.json",
  "examples/security-contract-proof/*.md",
  "examples/scope-fidelity-report/*.json",
  "examples/scope-fidelity-report/*.md",
]

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]
